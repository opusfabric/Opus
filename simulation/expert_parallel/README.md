# Paper Figure 14

From the repository root:

```bash
./simulation/scripts/build_backends.sh
./simulation/expert_parallel/run_ep.sh all
```

Use `run_ep.sh 256` or `run_ep.sh 512` to regenerate one system size.
